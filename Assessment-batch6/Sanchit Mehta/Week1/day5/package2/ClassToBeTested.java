package com.package2;
 
public class ClassToBeTested {  
	
    public static int findMax(int arr[]){  
        int max=0;  
        for(int i=1;i<arr.length;i++){  
            if(max<arr[i])  
                max=arr[i];  
        }  
        return max;  
    }
    public static int findMin(int arr[]) {
    	int min=;
    	for(int i=)
    }
    public static int access(int arr[],int idx) {
    	return arr[idx];
    }
    public static Object avg(int arr[]) {
    	if(arr.length==0)
    		return null;
    }
}  